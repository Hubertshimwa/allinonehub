"use client";

import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { Bell, Bookmark, Compass, LayoutDashboard, LogOut, Menu, MessageSquareText, PlusCircle, Search, Settings, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { Logo } from "@/components/logo";
import { Button } from "@/components/ui/button";
import { createClient } from "@/lib/supabase/client";
import { cn, getInitials } from "@/lib/utils";

const items = [
  { href: "/dashboard", label: "Overview", icon: LayoutDashboard }, { href: "/dashboard/discover", label: "Discover", icon: Compass }, { href: "/dashboard/bookmarks", label: "Saved", icon: Bookmark }, { href: "/dashboard/community", label: "Community", icon: MessageSquareText }, { href: "/dashboard/submit-opportunity", label: "Submit an opportunity", icon: PlusCircle },
];
export function DashboardShell({ children, user }: { children: React.ReactNode; user: { fullName: string; email: string; role: string } }) { const [open, setOpen] = useState(false); const pathname = usePathname(); const router = useRouter(); async function signOut() { try { await createClient().auth.signOut(); } finally { router.replace("/"); router.refresh(); } } const content = <><aside className="flex h-full w-72 flex-col border-r border-slate-200 bg-white p-5"><Logo /><nav className="mt-10 space-y-1">{items.map((item) => { const Icon = item.icon; const active = item.href === "/dashboard" ? pathname === item.href : pathname.startsWith(item.href); return <Link onClick={() => setOpen(false)} key={item.href} href={item.href} className={cn("flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold transition", active ? "bg-blue-50 text-blue-700" : "text-slate-600 hover:bg-slate-50 hover:text-slate-950")}><Icon className="size-4" />{item.label}</Link>; })}</nav><div className="mt-auto border-t border-slate-100 pt-4"><Link href="/dashboard/settings" className="flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-50"><Settings className="size-4" />Settings</Link>{["admin", "super_admin", "moderator"].includes(user.role) && <Link href="/dashboard/admin" className="flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-50"><ShieldCheck className="size-4" />Administration</Link>}<button onClick={signOut} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-slate-600 hover:bg-red-50 hover:text-red-600"><LogOut className="size-4" />Sign out</button><Link href="/dashboard/settings" className="mt-4 flex items-center gap-3 rounded-2xl bg-slate-50 p-3"><span className="grid size-9 place-items-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">{getInitials(user.fullName)}</span><span className="min-w-0"><span className="block truncate text-sm font-bold text-slate-800">{user.fullName}</span><span className="block truncate text-xs text-slate-500">{user.email}</span></span></Link></div></aside></>; return <div className="min-h-screen bg-slate-50"><div className="hidden fixed inset-y-0 left-0 z-30 lg:block">{content}</div>{open && <div className="fixed inset-0 z-50 lg:hidden"><div className="absolute inset-0 bg-slate-950/30" onClick={() => setOpen(false)} /><div className="relative h-full shadow-2xl">{content}</div></div>}<div className="lg:pl-72"><header className="sticky top-0 z-20 flex h-[72px] items-center justify-between border-b border-slate-200 bg-white/85 px-5 backdrop-blur sm:px-8"><Button variant="ghost" size="icon" className="lg:hidden" onClick={() => setOpen(true)}><Menu className="size-5" /></Button><div className="hidden items-center gap-2 text-sm text-slate-500 sm:flex"><Search className="size-4" /><span>Search Allinonehub</span><kbd className="ml-4 rounded border border-slate-200 px-1.5 py-0.5 text-[10px]">⌘ K</kbd></div><div className="flex items-center gap-2"><Button variant="ghost" size="icon" aria-label="Notifications"><Bell className="size-5" /></Button><span className="grid size-9 place-items-center rounded-full bg-blue-100 text-xs font-bold text-blue-700">{getInitials(user.fullName)}</span></div></header><main className="p-5 sm:p-8">{children}</main></div></div>; }
