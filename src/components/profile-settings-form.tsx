"use client";
import { FormEvent, useState } from "react";
import { CheckCircle2, Loader2 } from "lucide-react";
import { createClient } from "@/lib/supabase/client";
import { Button } from "@/components/ui/button";
export function ProfileSettingsForm({ profile }: { profile: { fullName: string; bio: string; location: string; interests: string[] } }) { const [state, setState] = useState<"idle" | "loading" | "success" | "error">("idle"); const [message, setMessage] = useState(""); async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setState("loading"); const form = new FormData(event.currentTarget); try { const supabase = createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) throw new Error("Please sign in again."); const interests = String(form.get("interests") ?? "").split(",").map((item) => item.trim()).filter(Boolean).slice(0, 10); const { error } = await supabase.from("profiles").update({ full_name: form.get("fullName"), bio: form.get("bio") || null, location: form.get("location") || null, interests, updated_at: new Date().toISOString() }).eq("id", user.id); if (error) throw error; setState("success"); setMessage("Your profile is updated."); } catch (error) { setState("error"); setMessage(error instanceof Error ? error.message : "We couldn’t save your profile."); } } return <form onSubmit={save} className="space-y-5"><Field label="Display name" name="fullName" defaultValue={profile.fullName} /><Field label="Location" name="location" defaultValue={profile.location} placeholder="e.g. Kigali, Rwanda" required={false} /><div><label htmlFor="bio" className="mb-1.5 block text-sm font-semibold text-slate-700">A little about you</label><textarea id="bio" name="bio" defaultValue={profile.bio} maxLength={280} rows={4} placeholder="What are you learning or working toward?" className="w-full rounded-xl border border-slate-200 px-3 py-3 text-sm outline-none focus:border-blue-400" /></div><div><label htmlFor="interests" className="mb-1.5 block text-sm font-semibold text-slate-700">Interests</label><input id="interests" name="interests" defaultValue={profile.interests.join(", ")} placeholder="Technology, scholarships, entrepreneurship" className="h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-blue-400" /><p className="mt-1.5 text-xs text-slate-500">Separate interests with commas. This helps us tailor recommendations.</p></div>{message && <p className={`flex items-center gap-2 rounded-xl px-3 py-2 text-sm ${state === "success" ? "bg-emerald-50 text-emerald-700" : "bg-red-50 text-red-700"}`}>{state === "success" && <CheckCircle2 className="size-4" />}{message}</p>}<Button variant="primary" type="submit" disabled={state === "loading"}>{state === "loading" && <Loader2 className="size-4 animate-spin" />}Save changes</Button></form>; }
function Field({ label, name, defaultValue, placeholder, required = true }: { label: string; name: string; defaultValue: string; placeholder?: string; required?: boolean }) { return <div><label htmlFor={name} className="mb-1.5 block text-sm font-semibold text-slate-700">{label}</label><input id={name} name={name} defaultValue={defaultValue} placeholder={placeholder} required={required} className="h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-blue-400" /></div>; }
