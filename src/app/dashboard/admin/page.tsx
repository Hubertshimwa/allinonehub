import { redirect } from "next/navigation";
import { AlertTriangle, Check, FileText, UsersRound } from "lucide-react";
import { getCurrentUser } from "@/lib/auth";
import { Button } from "@/components/ui/button";
export default async function AdminPage() { const user = await getCurrentUser(); if (!user || !["admin", "super_admin", "moderator"].includes(user.role)) redirect("/dashboard"); return <><div><p className="text-sm font-semibold text-blue-600">MODERATION CENTRE</p><h1 className="mt-1 text-3xl font-black tracking-[-.04em] text-slate-950">Keep the ecosystem healthy.</h1><p className="mt-2 text-slate-600">Review submissions, reports, and community signals with care.</p></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><AdminMetric icon={FileText} value="12" label="Pending opportunities" color="bg-blue-100 text-blue-600" /><AdminMetric icon={AlertTriangle} value="3" label="Open reports" color="bg-amber-100 text-amber-600" /><AdminMetric icon={UsersRound} value="28,412" label="Active members" color="bg-violet-100 text-violet-600" /></div><section className="mt-6 rounded-3xl border border-slate-200 bg-white p-6"><div className="flex items-center justify-between"><div><h2 className="font-black text-slate-950">Review queue</h2><p className="mt-1 text-sm text-slate-500">Recent opportunity submissions waiting for a trusted review.</p></div><Button variant="outline" size="sm">View all</Button></div><div className="mt-5 space-y-3">{["Women in Data Science Fellowship", "Remote Customer Success Internship", "Green Innovation Grant"].map((item, index) => <div key={item} className="flex flex-col gap-3 rounded-xl bg-slate-50 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-bold text-slate-800">{item}</p><p className="mt-1 text-xs text-slate-500">Submitted {index + 1} hour{index ? "s" : ""} ago · Official URL supplied</p></div><div className="flex gap-2"><Button variant="outline" size="sm">Review</Button><Button variant="soft" size="sm"><Check className="size-3.5" />Approve</Button></div></div>)}</div></section></>; }
function AdminMetric({ icon: Icon, value, label, color }: { icon: typeof FileText; value: string; label: string; color: string }) { return <div className="rounded-2xl border border-slate-200 bg-white p-5"><span className={`grid size-10 place-items-center rounded-xl ${color}`}><Icon className="size-5" /></span><p className="mt-4 text-2xl font-black text-slate-950">{value}</p><p className="mt-1 text-sm font-semibold text-slate-600">{label}</p></div>; }
