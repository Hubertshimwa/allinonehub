import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, BookOpen, CheckCircle2, Clock3, GraduationCap, PlayCircle, Star } from "lucide-react";
import { PageHero } from "@/components/page-hero";
import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { learningPaths } from "@/lib/constants";

export const metadata: Metadata = { title: "Learn practical skills" };
const resources = [{ title: "Write a personal statement people remember", category: "Scholarship guide", time: "12 min read", icon: GraduationCap, color: "bg-violet-100 text-violet-700" }, { title: "Your first portfolio: a simple, useful blueprint", category: "Career guide", time: "9 min read", icon: BookOpen, color: "bg-blue-100 text-blue-700" }, { title: "Freelancing safely: value, boundaries, and payments", category: "Money guide", time: "15 min read", icon: Star, color: "bg-amber-100 text-amber-700" }];
export default function LearnPage() { return <><SiteHeader /><main><PageHero eyebrow="Learn for real life" title={<>Knowledge is more useful when you can <span className="text-gradient">use it today.</span></>} description="Short, practical pathways that help you make confident next moves — at your own pace." /><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="flex items-end justify-between"><div><Badge className="bg-blue-50 text-blue-700">Guided pathways</Badge><h2 className="mt-4 text-3xl font-black tracking-[-.04em] text-slate-950">Start with a direction.</h2></div><Link href="/auth/sign-up" className="hidden text-sm font-bold text-blue-600 sm:block">Save your progress <ArrowRight className="inline size-4" /></Link></div><div className="mt-8 grid gap-5 lg:grid-cols-3">{learningPaths.map((path, index) => { const Icon = path.icon; return <article key={path.title} className="group overflow-hidden rounded-3xl border border-slate-200 bg-white"><div className={`h-36 bg-gradient-to-br ${path.accent} p-6 text-white`}><div className="flex items-start justify-between"><span className="grid size-11 place-items-center rounded-xl bg-white/20"><Icon className="size-5" /></span><span className="rounded-full bg-black/10 px-3 py-1 text-xs font-bold">{path.lessons} lessons</span></div></div><div className="p-6"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Path {String(index + 1).padStart(2, "0")}</p><h3 className="mt-2 text-xl font-black text-slate-950">{path.title}</h3><p className="mt-3 text-sm leading-6 text-slate-600">A clear starting point, useful practice, and tools you can return to.</p><Button variant="outline" className="mt-6 w-full" asChild><Link href={`/learn/${index + 1}`}><PlayCircle className="size-4" />Start learning</Link></Button></div></article>; })}</div></section><section className="bg-slate-50"><div className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div><Badge className="bg-emerald-100 text-emerald-700">Quick wins</Badge><h2 className="mt-4 text-3xl font-black tracking-[-.04em] text-slate-950">Useful reading, right now.</h2></div><div className="mt-8 grid gap-4 lg:grid-cols-3">{resources.map((resource) => { const Icon = resource.icon; return <Link href="/learn" key={resource.title} className="group rounded-2xl border border-slate-200 bg-white p-5 transition hover:-translate-y-1 hover:shadow-lg"><span className={`grid size-10 place-items-center rounded-xl ${resource.color}`}><Icon className="size-5" /></span><p className="mt-5 text-xs font-bold uppercase tracking-wider text-slate-400">{resource.category}</p><h3 className="mt-2 font-bold leading-6 text-slate-950">{resource.title}</h3><p className="mt-4 flex items-center gap-1 text-xs text-slate-500"><Clock3 className="size-3.5" />{resource.time}</p></Link>; })}</div></div></section><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="grid rounded-3xl bg-blue-600 p-8 text-white lg:grid-cols-[1fr_.9fr] lg:p-12"><div><p className="text-sm font-bold text-blue-200">YOUR LEARNING LIBRARY</p><h2 className="mt-4 text-3xl font-black tracking-[-.04em]">Build a profile that learns with you.</h2><p className="mt-4 max-w-lg leading-7 text-blue-100">Save resources, track small wins, and receive ideas based on what you want to achieve.</p><Button className="mt-7 bg-white text-blue-700 hover:bg-blue-50" asChild><Link href="/auth/sign-up">Join free <ArrowRight className="size-4" /></Link></Button></div><div className="mt-8 space-y-4 lg:mt-0"><Feature text="Track your progress without pressure" /><Feature text="Save every useful resource in one place" /><Feature text="Get simple recommendations when you need them" /></div></div></section></main><SiteFooter /></>; }
function Feature({ text }: { text: string }) { return <div className="flex items-center gap-3 rounded-xl bg-white/10 p-4 text-sm font-semibold"><CheckCircle2 className="size-5 text-blue-200" />{text}</div>; }
